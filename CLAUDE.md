# 営業管理ツール vol3 - 仕様書

## プロジェクト概要
React.js + Firebase で構築された営業進捗管理システム。管理者用とパートナー用の2つの独立したアプリケーションを提供。

## 技術スタック
- **フロントエンド**: React 19.1.0, styled-components, react-router-dom
- **バックエンド**: Firebase (Firestore, Hosting)
- **AI機能**: OpenAI GPT-4o-mini（議事録分析）
- **認証**: SHA256暗号化、セッション管理
- **デプロイ**: Firebase Hosting

## アクセス情報
- **管理者用**: https://sales-management-staging.web.app (ID: salessenjin / PW: salessenjin1234)
- **パートナー用**: https://sales-management-staging.web.app/partner/ (ID: salessenjinpiala / PW: salessenjinpiala1234)

## 主要機能

### 1. 営業管理機能
- **ホームダッシュボード**:
  - チーム全体四半期売上実績（目標達成率メーター）
  - 四半期内月別売上実績（棒グラフ）
  - チーム全体売上見込み（担当者別円グラフ）
  - 個人四半期/月間売上
  - **担当者別案件サマリー（フェーズ2〜7）**: 担当者選択式、逆ピラミッド型ファネルチャート（案件数・予算）、案件一覧
  - 滞留商談リスト（90日以上経過）
  - メニュー別/会社別実績サマリー
- **アクションログ記録**: 商談履歴の記録、AI議事録分析
- **案件一覧**: 進捗状況の管理、フィルター機能（複数選択対応）、受注案件の次回アクション日付非表示
  - 次回アクション日付バッジ: 2日以内は「急」（赤）、超過は「超過」（紫）
- **カンバンボード**: ステータス別の案件管理（ドラッグ&ドロップ機能は削除）
- **成約案件管理**: 確定日ベースの売上管理

### 2. 案件管理機能（第一想起取れるくん専用）
- **投稿本数管理**: 月別・日別の投稿数記録、受注案件のみ表示、実施月ベース自動登録
- **投稿カレンダー**: 全体スケジュールの可視化
- **ToDo管理**: 営業日ベースのタスク自動生成、期限切れアラート表示、完了済みタスクの「タスクDone」セクション
- **週報**: 四半期KGI/KPI設定と週次進捗入力、サービス別実績の自動集計

### 3. マスター管理
- **紹介者マスター**: 紹介者情報の管理
- **提案メニューマスター**: 提案メニューのカスタマイズ
- **インフルエンサー管理**: CSV一括登録対応
- **キャスティング管理**: IFキャスティング案件の進捗管理

### 4. パートナー専用機能
- 部署別管理（Buzz、コンサル、デジコン、マーケD）
- 担当者別目標管理（月別目標件数・想定遷移率）
- 紫系デザインの独立したUI

## Firestore データ構造

### コレクション一覧
```javascript
// 案件進捗
progressDashboard: {
  productName: string,          // 商材名
  proposalMenu: string,         // 提案メニュー
  representative: string,       // 社内担当者
  partnerRepresentative: string,// パートナー担当者
  introducer: string,          // 紹介者
  status: string,              // ステータス
  confirmedDate: string,       // 確定日（成約日）
  sub_department_name: string, // 部署名
  sub_department_owner: string,// 他部署担当者
  createdAt: timestamp,        // 案件登録日
  leadTimeDays: number,        // リードタイム（登録〜成約/失注までの日数）
  lostAtPhase: string,         // 失注時の直前フェーズ（失注案件のみ）
  lostDate: string,            // 失注日（失注案件のみ）
  stageProgress: {             // 受注後の進行ステージ（運用管理・既存案件のみ。confirmedDate >= 2026-07-15の案件のみ対象）
    currentStage: number,      // 現在のステージ（1〜7）
    completedAt: {             // ステージ番号 → 完了日時（Done押下時のserverTimestamp）
      [stageNo]: timestamp | null
    }
  },
  stageNaBackfilled: boolean,  // ステージ連動NAのバックフィル実行済みフラグ（2026年8月の一回限りスクリプトで付与）
  phase4StagnationNaCreatedAt: timestamp, // フェーズ4滞留NA（決済者に直接話すことを提案する）の生成済みフラグ。
  // 一度立てたら二度と消さない（ステータス変更でもリセットしない）。Timestamp.now()の確定値で書く
  // （serverTimestamp()だと遅延補償でnullに読めて無限重複生成が起きた経緯あり。詳細はsrc/utils/phase4StagnationNa.js）
  // ステージ連動NA: 最新salesRecordのentriesに決定的ID "stageNa_{ステージ番号}" で保存。
  // エントリに stageNaStage: number を持ち、通常NAと区別する（内容文字列でのマッチはしない）
  billing: {                   // 契約・請求管理（運用管理のみ。自社→クライアントの請求額管理）
    paymentTerms: {
      closingDay: string,      // 締め日（自由入力: "月末"、"15日"など）
      paymentSite: string      // 支払サイト（自由入力: "翌月末"など）
    },
    contracts: [{              // 契約期間（複数、表示は開始日昇順）
      id: string,              // 期間の一意ID
      startDate: string,       // 開始日 "YYYY-MM-DD"
      endDate: string,         // 終了日（未定は空文字）
      unitPrice: number,       // 単価（円・小数可）
      unitTarget: string,      // 単価の対象（自由入力: "再生数"など）
      monthlyBudget: number,   // 月次予算額（円）
      budgetPolicy: string,    // "上限厳守" | "超過可"（BUDGET_POLICIES）
      actuals: [{              // 月次実績（複数、表示は年月昇順）
        yearMonth: string,     // "2025-10"形式
        quantity: number,      // 実績数量（再生数など）
        manualAmount: number | null // 手動上書き請求額。null=自動計算
        // 自動計算の請求額は保存しない（数量×単価を1円未満切り捨て、上限厳守なら予算で頭打ち、で都度計算）
      }]
    }]
  },
  // ...
}

// アクションログ
actionLogs: {
  dealId: string,              // 案件ID
  productName: string,         // 商材名
  action: string,              // アクション内容
  description: string,         // 議事録
  summary: string,             // AI要約
  // ...
}

// 投稿スケジュール（第一想起取れるくん専用）
postingSchedules: {
  dealId: string,              // 案件ID
  dealName: string,            // 案件名
  yearMonth: string,           // "2025-07"形式
  schedules: { [day]: number } // 日別投稿数
}

// タスクテンプレート（第一想起取れるくん専用）
taskTemplates: {
  dealId: string,              // 案件ID
  receivedOrderDate: string,   // 受注日（確定日）
  startDate: string,           // 開始予定日
  tasks: [{                    // タスクリスト（7個の固定タスク）
    name: string,              // タスク名
    taskDate: string,          // 実施予定日
    completed: boolean,        // 完了フラグ
    completedDate: string      // 完了日時
  }]
}

// 週報設定（四半期KGI/KPI）
weeklyReportSettings: {
  quarterId: string,           // "2025-Q4"形式
  kgis: [{
    id: string,
    name: string,              // KGI名
    target: number,            // 四半期目標値
    unit: string,              // 単位（円、件など）
    autoAggregate: boolean,    // 自動集計フラグ
    aggregateService: string,  // 集計対象サービス
    aggregateMetric: string,   // 集計指標（sales, closedCount等）
    kpis: [{                   // 紐付くKPI
      id: string,
      name: string,
      target: number,
      unit: string
    }]
  }]
}

// 週報データ
weeklyReports: {
  weekId: string,              // "2025-11-18"形式（火曜日の日付）
  weekStart: string,           // 週の開始日時
  weekEnd: string,             // 週の終了日時
  kgiValues: { [kgiId]: number }, // KGI今週実績
  kpiValues: { [kpiId]: number }, // KPI今週実績
  kgiCumulatives: { [kgiId]: number }, // KGI四半期累計（先週累計+今週実績）
  kpiCumulatives: { [kpiId]: number }, // KPI四半期累計（先週累計+今週実績）
  actions: [{                  // 今週の定量アクション（目標値なし）
    id: string,
    content: string            // アクション内容のみ
  }],
  previousActionResults: [{    // 先週アクションの結果（先週のactionsから自動引き継ぎ）
    id: string,
    content: string,           // 先週のアクション内容
    result: string,            // ◯、△、×
    resultNote: string         // 備考
  }],
  review: string,              // 振り返り（文字数制限なし）
  updatedAt: string
}

// デイリータイマー（担当者×日付で1ドキュメント、ID: "{担当者名}_{YYYY-MM-DD}"）
dailyTimers: {
  representative: string,      // 担当者名
  date: string,                // "2026-07-31"形式
  tasks: [{                    // タスク行
    id: string,                // 行ID
    name: string,              // タスク名
    plannedMinutes: number,    // 予定時間（分・整数、必須。新規追加時は0件許容しない。既存の古いnullデータは互換のため残る）
    plannedStartTime: string,  // 予定開始時刻（"HH:MM"、未設定はnull。実績とは別物で編集可）
    sessions: [{               // 作業区間（時系列順）。再開のたびに区間が増える
      startedAt: timestamp,    // 区間の開始（開始/再開ボタン押下時刻）
      endedAt: timestamp       // 区間の終了（実行中はnull。開いている区間は常に最後の1つのみ）
    }],
    outputUrls: [string],      // アウトプットのリンク（任意。なしは未定義/空配列。https://を自動補完して保存）
    overrunReflection: string, // 振り返りウィザード手順1で記入した「大幅超過の振り返り」（任意フィールドなので未記入行は未定義）
    recoveryPlanned: boolean,  // 振り返りウィザード手順2でリカバリー方法（新期日/早起き）を決定済みかのフラグ
    meetingLink: {             // 振り返りウィザード手順5で追加した、議事録が自動記録されるミーティング由来のタスクの参照（任意）
      dealId: string, companyName: string, meetUrl: string,
      meetingType: string,     // '定例' | '臨時'
      scheduledDate: string, startTime: string | null
    }
    // 旧形式はstartedAt/endedAt直持ち（区間1つとして解釈し、いずれかの操作時に新形式へ変換）
  }],
  manualSort: boolean,         // trueならtasks配列の並びが表示順の正（D&Dで初めて並び替えた時に立つ。未設定は予定開始時刻ソート表示）
  review: {                    // 1日1件の振り返り（tasksとは独立、担当者×日付ごと）。振り返りウィザードの手順0・3・4の「確認しました」フラグ・任意の自由記述を持つ
    reminderAcked: boolean,        // 手順0: リマインド頻度を確認した
    pipelineStatusChecked: boolean,// 手順3: 各案件のステータス確認をした
    pipelineWeekChecked: boolean,  // 手順4: 今週確定予定の案件確認をした
    pipelineStatusNote: string,    // 手順3の振り返りコメント（任意、空文字許容）
    pipelineWeekNote: string       // 手順4の振り返りコメント（任意、空文字許容）
  },
  reviewCompletedAt: timestamp,// 夜の振り返り完了の正（Cloud Function POST /api/staff/night-review-complete が設定）。存在有無のみで完了判定
  timeAccuracy: {               // タイマー止め忘れ・つけ忘れの記録（functions/dailyReportGuard.jsが日々積み上げ）
    reminderCount: number,          // リマインド回数（超過・放置リマインドの合算。種別は分けていない）
    inaccurateMinutes: number       // 不正確だった時間（超過分＋タイマー未開始で空いていた時間）
  },
  updatedAt: timestamp
  // 実績時間は保存しない（閉じた作業区間の合算を都度計算する）
}
```

## ビルド・デプロイ

```bash
# 両アプリ同時ビルド
./build-all.sh

# デプロイ
firebase deploy --only hosting --project sales-management-staging

# 個別ビルド
npm run build                    # 管理者用
REACT_APP_ENTRY_POINT=partner npm run build  # パートナー用
```

## 最新バージョン情報
- **現在バージョン**: v2.49.0
- **最終更新**: 2026年9月8日
- **直近の更新内容**:
  - モール売上データの督促Slackメッセージに「今回は売上なし」ボタンを追加
    - `functions/mallUpdateGuard.js`が7日以上データ未更新のモール商品をSlackで督促する仕組み（既存）に、実際に売上が無く入稿するデータが無い場合向けのボタンを追加
    - 押すと`mallUpdateChecks/{checkId}`に`noSalesAckedDate`（押した日のJST日付）を記録し、実際に入稿されたのと同じ扱いにする（滞留日数の起算日を、実際の売上データの最終日と`noSalesAckedDate`の新しい方で判定するよう`mallUpdateGuard.js`の`staleDays`計算を変更）。stateは`watching`のままで、次にまた7日以上更新が無ければ通常通り再度督促する
    - ハンドラーは`functions/slackApproval.js`に追加（`mall_mark_no_sales`）。「確認送付」後の督促メッセージにも同じボタンを追加
  - 日報の振り返りウィザード手順5（翌日の予定作り）を左右2カラムに変更
    - 左＝NA候補・MTG候補・早起き候補・NA一覧・完了ボタン、右＝翌日の予定プレビュー（タイムライン）。画面幅に余裕があれば横並びになり、スクロールせずに予定の埋まり具合を確認しながら追加操作できる（`WizardSideBySide`を手順3・4と共通利用）
  - 日報の細かい改善3点
    - 時刻重複の判定を、予定確定前は予定時刻ベース・確定後は実績（sessions）ベースに変更。確定後は計画がずれるのが普通のため、実際に二重記録された時間だけを問題にする（`actualOverlappingTaskIds`）
    - 振り返りウィザード手順5（翌日の予定作り）の「定例ミーティング候補」を、商材（案件）単位ではなく会社単位で1件にまとめて表示するよう修正（`clientMeetingSettings`が会社単位の設定のため）
    - 手順5に、翌日の予定を通常の「予定の確認・確定」と同じ見た目のタイムラインでプレビュー表示する機能を追加（`PreviewTimelineGrid`）。空き時間も同様に表示され、追加するたびに更新される
  - 日報: タスクの予定時刻が他のタスクと重なっている行を、一覧上でその場でわかるように表示
    - 「予定を確定する」は空き時間・時刻重複のどちらもゼロでないと押せない仕様だが、重複は今まで一覧下部の警告文でしか気づけず、原因箇所の特定に手間がかかっていた
    - 該当するタスク行を赤枠・赤背景にし、「⚠️時刻重複」バッジを表示するように変更（全状態のタスク行に対応）。確定できない原因にその場で気づける
  - パイプライン振り返りページ「保有中の案件」の着地確率（確率欄）が0%のまま保存され続けるバグを修正
    - この欄は`weeklyForecasts/{週ID}.probability`として案件×週ごとに保存され、未設定ならフェーズ既定値（フェーズ1=5%など）にフォールバックする仕組み。しかし空欄のまま欄の外にクリックすると`Number('') || 0`により0%として保存されてしまい、以後その週はフェーズ既定値に戻らず0%のまま固定されるバグがあった
    - 修正: 空欄・数値でない入力のまま外れた場合は保存しないように変更（`handleProbabilityBlur`）
    - 既に0%で保存されてしまっている案件は、同じ欄に正しい%を入力し直せば直る（このバグ修正後は再発しない）
  - 営業記録（`salesRecords`/`newCaseSalesRecords`の`budget`）の入力を必須化（0円・空欄での保存を禁止）
    - 既存案件のパイプライン一覧の一部（`ExistingDealsDashboard.js`の滞留商談リスト・担当者別サマリー）が、案件本体の`expectedBudget`ではなく最新営業記録の`budget`を「想定予算」として表示しており、この`budget`は今までバリデーション無しで空欄・0円のまま保存できていたため0円表示が発生していた
    - `ProjectDetailPanel.js`の営業タブ（新規追加・既存行のインライン編集）で、0円・空欄の保存を禁止するよう修正
    - 案件本体の`expectedBudget`と営業記録ごとの`budget`は別々に手入力する独立したフィールドであり、自動転記は無い（詳細は本ファイルのデータ構造セクション参照）
  - 日報の振り返りウィザードを4点改善
    - 振り返り内の固定タスク（「日報に基づく振り返り」「週次パイプライン振り返り」「翌日の予定の記入」）のタイマーを、該当する手順に入ったタイミングで自動的に開始するように変更（手動での開始/終了操作が不要に。次のフェーズを開始するだけで前のフェーズは自動終了する）
    - 手順3・4（週次パイプライン振り返り）を、パイプライン振り返りページへの遷移をやめ、日報画面内で完結する形に変更。保有中案件のフェーズ・ネクストアクションや今週成約予定案件の確度を画面内に読み取り専用で表示しつつ、任意の自由記述欄（`review.pipelineStatusNote`/`pipelineWeekNote`）に振り返りを書けるようにした
    - 手順5（翌日の予定作り）に、翌日が定例ミーティング（`clientMeetingSettings.recurringDayOfWeek`が翌日の曜日と一致）または単発ミーティング（`progressDashboard/{dealId}/materials/slot_{翌日の日付}`が存在）にあたる案件を候補として表示し、ワンクリックで翌日プランに追加できるように追加（任意、追加しなくても完了できる）。追加したタスクには`meetingLink`が付き、タスク一覧に「🎥議事録自動記録」バッジが表示される（ダッシュボードに登録されていない社外ミーティングの登録漏れに気づきやすくする目的）
    - その日の振り返りが完了した後も、手順1（大幅超過タスクの振り返り）・手順2（未完了タスクのリカバリー）・手順3・4（自由記述）で記入した内容を、バッジの下に要約として表示し続けるように変更（今までは完了バッジのみで内容を見返せなかった）
    - `src/services/projectService.js`に`fetchDealsForRep`/`fetchAllClientMeetingSettings`/`fetchMaterialSlot`/`fetchPipelineReviewSnapshot`を追加
  - 案件登録・編集時の想定予算（`expectedBudget`）を必須化（0円・空欄での保存を禁止）
    - 保有中の案件に想定予算0円のものが存在していたバグを調査した結果、案件の新規登録・編集フォームで想定予算が唯一バリデーション対象外（他項目は必須チェックあり）になっており、空欄や「0」のまま保存できてしまっていたことが原因と判明
    - 対象は新規案件登録の全経路: `LogEntryPage.js`（アクションログ経由の新規登録）、`ProgressDashboard.js`（案件一覧の追加・編集モーダル）、`AccountDealsListPage.js`（アカウント営業の新規追加・既存案件追加・編集モーダル）、`AccountSalesDashboard.js`（旧アカウント営業画面の追加モーダル）
    - いずれも入力必須（0円・空欄は保存不可、ボタン非活性またはalertで案内）に変更。ラベルに「*」を追加、`min`属性も`0`→`1`に変更
    - CSV一括取込経路は今回対象外（既存のまま。空欄行があってもエラーにはならない）
  - パイプライン振り返りページ: 新規＋既存を合算した「四半期合計進捗」セクションを追加
    - 荒幡さん個人の四半期合計目標（新規＋既存の合算、1億円）に対して、確定実績・見込み込み（保有案件の想定予算×着地確率を加算）の2つを進捗バー付きで表示。今の四半期を見ている時だけ見込み込みの行を表示（過去・未来の四半期は確定実績のみ）
    - `salesTargets`コレクションはチーム全体の目標（新規・既存で別ドキュメント）であり担当者個人の目標ではないため、荒幡さん個人の目標は`REP_QUARTERLY_TARGET`定数として画面側に別途持つ（四半期が変わったら手動更新）
    - 確定実績は既存の`fetchRealizedRecords`の結果（新規・既存の両方を含む、今は`recordsForType`でタブ別に絞っていたもの）をそのまま合算するだけで新規フェッチ不要。見込みは新規/既存タブの選択に依存しないよう、両方の保有中案件を対象にした専用の軽量フェッチを1つ追加（既存の`loadData`・タブ別表示のロジックは変更なし）
    - 新規・既存の内訳も合計の下に併記
  - 日報: 振り返りウィザードの細部を調整（時刻・入力必須化・表示まわり）
    - 振り返りの固定枠（`isReviewTask`）を復活。ウィザードの3つの塊にそのまま対応する「日報に基づく振り返り」（23:00・10分）「週次パイプライン振り返り」（23:10・20分）「翌日の予定の記入」（23:30・20分）を`functions/dailyReportGuard.js`が平日分先2週間ぶん自動で用意する（前回いったん廃止していたが、名前・時間配分を新ウィザードに合わせた形で復元）
    - 夜チェックの開始時刻を23:20→**23:00**に変更（`REVIEW_KICKOFF_HOUR/MINUTE`）。連動して朝の「予定を確定する」の締め時刻（`PLAN_WINDOW_END`、`src/utils/dailyTimerSchedule.js`）も23:00に変更
    - タスク追加（日報のメイン「タスクを追加」フォーム、確定後の追加、翌日プランの手動追加行）で予定時間の入力を必須化（旧: 任意）。ラベルの「（任意）」表記も削除
    - 手順1（時間の使い方の振り返り）: 次のアクションの追加を任意化し、タスクごとに複数追加できるように変更（振り返り本文のみ必須のまま）
    - 手順0の表示で、タイマー停止時間の合計が小数（`191.29999999999995分`のような浮動小数点誤差）で出ていたのを整数に丸めて表示するよう修正（`computeFreeRanges`で分数を丸める）。あわせてリマインド回数等の数字を案内文より大きく表示するよう調整
    - 夜の振り返り督促（Slack）の「開始済みか」判定を、廃止済みの固定タスクのタイマー操作ベースから、ウィザード手順0完了フラグ（`review.reminderAcked`）ベースに修正（新ウィザードはタスクのタイマーを使わずに進行するため、旧判定のままだと振り返り完了後も督促が止まらなくなっていた）
  - 日報: 23:20からの振り返りを、案内に沿って進める6手順のウィザードに作り直し
    - 「振り返りを始める」ボタンを押すと自動で手順0→5に案内され、全部終わるとその日の振り返り完了→翌日の予定記入→ページが翌日に進む、まで一続きで進行
    - 手順0（リマインド確認）: `timeAccuracy.reminderCount`（合計回数）とタイマー停止回数・時間（`freeGaps`から算出、表示専用）を見て「確認しました」を押すだけ
    - 手順1（時間の使い方の振り返り）: 超過タスクのうち`functions/dailyReportGuard.js`の`isOverrun`と同じ基準で「大幅に超過」したものだけを対象に振り返り本文の入力を必須化（軽微な超過は一覧表示のみ）。振り返りはタスク行の`overrunReflection`に保存。次のアクション（名前・予定時間・期日）は任意・タスクごとに複数追加可（追加ボタンを押したものだけが翌日プランに積まれる）
    - 手順2（未完了タスクの振り返り）: 未完了タスクごとに「新しい期日にする」か「明日早起きして片付ける」を選択必須（早起きの時刻はここでは決めず手順5で指定）。決定済みフラグを`recoveryPlanned`に保存
    - 手順3・4（週次パイプライン振り返り）: パイプライン振り返りページを別タブで開いて目視確認する方式（重い埋め込みは行わない）。確認で`review.pipelineStatusChecked`/`pipelineWeekChecked`を記録、手順4の確認で夜の振り返り自体も完了（`completeNightReview`）
    - 手順5（翌日の予定作り）: 期日が明日の案件NA（必須）・2〜3日以内の案件NA（任意）・手順1/2で作ったアクションを表示。早起き候補は開始時刻の指定が必須。翌日プランの予定に空きがあると完了できない（`computeScheduleGaps`を流用）。完了で`planNextDayTasks`を実行しページが翌日に切り替わる
    - `review`のスキーマを`{reminderAcked, pipelineStatusChecked, pipelineWeekChecked}`の3フラグに簡素化し、旧来の自由記述4項目（達成できなかったこと/時間の使い方/振り返り/NA）は廃止
    - リマインド回数は種別（超過起因/放置起因）を分けず合計のみ表示（`timeAccuracy.reminderCount`の集計ロジック自体は変更なし）
    - 調査のみ実施・今回は未修正: 緊急クエスト（🚨）まわりに複数の改善余地あり（予定確定時のタイムライン描画・リアルタイム反映・完了報告の二重防止・繰越時のバッジ引き継ぎ等）
  - 日報: `functions/dailyReportGuard.js`の夜の振り返り督促を、廃止した固定タスク方式からウィザード連動に修正
    - 旧バージョンは「振り返りの4区切り」（Q3新規の振り返り／Q3既存の振り返り／今日のアクションの振り返り／明日の日程記入、各10分・23:20〜23:50スタート）を`isReviewTask`の固定タスクとして先2週間分あらかじめ`dailyTimers.tasks`に用意し、そのどれかにタイマーが入ったこと（`sessions`が増えたこと）を「振り返りを開始した」の判定に使っていた
    - 上記の6手順ウィザードはタスクのタイマーを一切使わず進行するため、この判定が永久にtrueにならず、督促（10分おきにエスカレーションする「振り返りを始めてください」）が振り返り完了後も止まらなくなる不具合があった（ユーザー報告: 「固定で入る振り返りが今の新しい振り返りにあってない」）
    - 修正: 固定タスクの自動生成そのものを廃止し、まだ着手されていない古い固定タスクは実行のたびに掃除するだけにした。「開始済みか」の判定はウィザード手順0完了のフラグ`dailyTimers.review.reminderAcked`を見る方式に変更（未開始なら「開始してください」、開始済みなら「最後まで進めて完了してください」を出し分け、`reviewCompletedAt`が立つまで督促を続ける）
    - Slackの開始連絡メッセージも「振り返りは4つに分かれています」という旧文言から、「「振り返りを始める」ボタンを押すと手順に沿って進められます（目安合計50分）」に変更
    - 着手済み（`sessions`が残っている）過去の固定タスクは記録として削除しない
  - パイプライン振り返り: 月別実績の重複表示バグを修正（案件ごとに`isExistingProject`に対応する片方の営業記録サブコレクションのみを読むように統一）。保有中の案件をフェーズが大きい（成約に近い）順に表示。週選択に常に「来週」まで表示されるよう追加
  - 成約案件一覧の重複行バグを修正（受注保存処理を`saveReceivedOrder`（`projectService.js`）に共通化）
    - 成約案件一覧は「phase=フェーズ8の営業レコード1件=1行」。受注時に①営業記録タブが現在レコードのphaseをフェーズ8に更新②受注モーダルが成約レコードを無条件追加、の2系統からレコードが生まれ、既存案件では一度の受注で2行になっていた（7に戻して再受注すると+1）
    - 修正: モーダル保存時、salesRecords内に「confirmedDateを持たないフェーズ8レコード」（タブ由来）があれば最新1件に受注情報を上書きして成約レコードへ昇格（date・recordTypeは元レコードを維持）。confirmedDate付きの確定済み成約は保護し新規追加
    - 受注保存の対象案件をselectedDeal（パネルで開いている案件）からorderData.dealId（モーダルに渡した案件）に修正。パネル未オープン時に何も保存されない・別案件パネルを開いたまま操作すると誤った案件へ書き込まれるバグを解消（案件一覧・アカウント営業一覧）
    - Partner系（案件進捗・カンバン2種）の受注保存で、現行モーダルが渡さない`receivedOrderMonth`（undefined）でupdateDocが失敗していたのを受注日から実施月を導出する形で修正
    - アカウント営業の受注は「新規側を残し既存側に複製ドキュメントを作る」方式を意図的に維持（通常案件のin-place移行とは別設計）。新規側のタブ由来フェーズ8レコードが成約一覧に重複表示され得る問題は未解決の既知課題
    - 補足: 新規案件と既存案件は同一ドキュメント（in-place移行）のため、片方の一覧から削除すると両方から消えるのは仕様
  - NA管理: 期日当日・超過のtodoを「本日必達」カラムへ自動表示
    - カラム振り分け時の導出のみでDBの`actionStatus`は書き換えない（旧feature/today-due-auto-moveのstash実装は、紹介者NAに案件NA用の更新パスを使いボードが読めなくなるバグ+全ユーザーload時一斉書き込みの構造だったため不採用。stashは未取り込みのまま）
    - 期日超過分も本日必達に含める。自動表示中のカードはtodoへ戻せない（アラート表示）。本日必達→レビュー待ち/doneへの移動は通常どおり
    - DB上はactiveのままのため案件詳細パネルのステータスバッジは「todo」表示（カンバン上の位置のみ変わる）
  - NA管理: doneドロップ時に次のNA入力を必須化
    - カードをdoneカラムにドロップすると「次のネクストアクション入力」モーダルを表示（NA内容・期日とも必須、スキップ不可）。確定で①元NAをdone化②同じ案件/紹介者の新NAをtodoに追加
    - ステータスはモーダル確定まで書き込まないため、キャンセル・オーバーレイクリックで閉じるとdoneへの移動自体が取り消される（ロールバック不要の設計）
    - ステージ連動NA（進行ステージ対象案件）は対象外＝現行動作のまま（done時に次ステージNAが自動生成されるため二重登録を防ぐ）。対象はカンバンのドロップ操作のみで、案件詳細パネル等の完了経路は変更なし
  - フェーズ4滞留NA「決済者に直接話すことを提案する」の大量重複生成バグを修正
    - 原因: 生成済みフラグを`serverTimestamp()`で書いていたため、サーバー確定前のローカルスナップショットでnullに読め（遅延補償のデフォルト挙動）、onSnapshot→生成→書き込み→onSnapshot→…の無限ループが成立していた。画面を開いている間NAが増え続け9000件超が蓄積
    - 修正: 生成ロジックを`src/utils/phase4StagnationNa.js`に共通化（案件一覧・アカウント営業一覧の2箇所にあったコピーを一本化）し、3重ガードに変更
      - ①フラグは`Timestamp.now()`の確定値で書く（pending中もtruthyに読める）②NA生成とフラグ書き込みを`writeBatch`で原子化 ③セッション内Setで同一案件への並行実行を遮断
    - 方針変更: 一度生成した案件には二度と生成しない。ステータス変更時のフラグリセット（案件一覧・アカウント営業一覧・案件パネルの3箇所）を全廃。完了済み・削除済みNAも「生成済み」扱い（判定の正は案件側フラグのみで、NAの現存確認はしない）
    - 蓄積していた重複NAは2026年8月11日に一回限りのクリーンアップスクリプトで削除済み（対象9,242件・4案件、各案件の最古1件のみ残して9,238件削除、全件フラグ付与確認済み）。スクリプトは`scripts/scanStagnationNa.mjs`（読み取り専用の調査）と`scripts/cleanupStagnationNa.mjs`（削除実行）。削除前の全件バックアップJSONは顧客名を含むためリポジトリには含めずローカル保管のみ（`scripts/backup-*.json`は.gitignore済み）
  - 日報: タスク一覧のドラッグ&ドロップ並び替えを追加
    - 各行左端のハンドル（⋮アイコン）からドラッグ、行の上半分/下半分で挿入位置（前/後）を判定し挿入線を表示。全状態（未開始・実行中・完了）の行を並び替え可能
    - 並び順は`tasks`配列の順序そのものが正。並び替え時に`reorderTasks`が配列を書き換え、ドキュメントに`manualSort: true`を立てる（sortOrder等の番号フィールドは持たない）
    - `manualSort`未設定の既存ドキュメントは従来どおり予定開始時刻ソートで表示（マイグレーション不要）。初回ドラッグ時に、その時点の表示順にドラッグ結果を適用して保存
    - 手動並び順の日は新規タスクが末尾に追加される（時刻による自動整列はしない）。行直下の「＋」差し込みは`addTask`の`afterTaskId`引数で配列上も押した行の直後に挿入され位置が保たれる
    - 予定時刻は一切変更しない（表示順のみ）。担当者をまたぐ移動は不可（データ上も担当者ごとに別ドキュメント）。ネイティブHTML5 D&Dのためタッチ操作は非対応（PC運用前提）
    - 並び替え保存時はタスクIDの過不足を検証し、他画面での同時変更を検知したらエラー（画面再読み込みを促す）
  - 日報: 各タスクにアウトプットリンクを登録できる機能を追加
    - タスク行の時刻編集フォーム（編集アイコン）に「アウトプットリンク（1行に1URL）」のtextareaを追加。改行区切りで複数URL登録可、`outputUrls: [string]`として保存（`http(s)://`なしは`https://`を自動補完、URL形式チェックあり）
    - サービス層は`updateTaskSessions`を`updateTaskDetails({sessionTimes, outputUrls})`に拡張。時刻欄がすべて空なら`sessionTimes: null`でsessionsは変更せずリンクのみ保存（未開始タスクにリンクだけ登録できる）
    - 一覧ではリンクがある行にのみリンクアイコンを表示。1件ならクリックで直接新しいタブで開く、複数件なら件数バッジ付きでポップオーバーからリンクを選んで開く（一括で開かない=ポップアップブロック対策）
    - 既存タスク（`outputUrls`未定義）は空扱いで動作（マイグレーション不要）
  - 日報: 各タスク行に「＋」ボタン（行直下へのタスク差し込み）を追加
    - 押すとその行の直下にインライン入力欄（開始時刻・タスク名・所要時間）が開く。開始時刻には押した行の予定終了時刻（予定開始+予定時間）を自動入力、計算できない行（予定なし等）は空で開き手入力（開始時刻・タスク名・所要時間すべて必須）
    - 追加は既存の`addTask`をそのまま使用（サービス層変更なし）。追加先は押した行の担当者。既存タスクの予定には影響しない
    - 同時に開けるフォームは1つ（時刻編集フォームとも排他）。並び順は既存の予定開始時刻昇順に従う
  - 日報: 完了タスク行の右端に妥当性の◯×を自動表示
    - 実績が予定時間以内=◯（緑）、超過=×（赤）、予定時間なし=−（グレー）。未開始・実行中は表示しない
    - 判定は超過バッジ・超過タスク一覧と同じ`getTaskTiming`の`overdue`を使用（表示間で矛盾しない）。手動変更は不可、保存もしない（表示のみ）
  - 日報: 割り込みタスクの「今すぐ開始」ボタンを追加
    - 「タスクを追加」フォームの「追加」ボタン横に緑の「今すぐ開始」ボタン（今日の日付を表示中のみ）。押すと現在時刻を開始時刻としてタスクが追加され、即実行中になる（`addTaskAndStart`）
    - 予定開始時刻は使わない（`plannedStartTime: null`で保存、既存タスクの予定にも影響なし）。予定時間はフォームに入力されていれば使う
    - 実行中のタスクは自動終了（同時実行1タスクの不変条件を維持）。中断されたタスクは「再開」で戻れる
  - 日報: 作業区間の開始・終了時刻の手動修正機能を追加（タイマー押し忘れの事後修正用）
    - 各行の編集アイコン（全状態・過去日でも表示）からインライン編集。区間ごとに開始・終了の時刻（"HH:MM"、その日付内、秒は00）を修正でき、実績は修正後の区間から都度再計算
    - 実行中タスクは開始時刻の修正に加え、終了時刻を入力するとその時刻で完了（空のままなら実行中を継続）
    - 未開始タスクは開始・終了の両方を入力すると完了として記録。手動修正で実行中状態は作らない
    - バリデーション（`updateTaskSessions`）: 各区間で終了>開始、区間同士は時系列順で重複不可、日またぎ不可。区間の追加・削除は編集ではできない（区間を増やしたい場合は「再開」してから時刻を修正する運用）
  - 日報: 終了したタスクの再開機能を追加
    - タスクのデータモデルを作業区間の配列`sessions: [{startedAt, endedAt}]`に変更。実績時間は閉じた区間の合算を都度計算（保存しない設計は維持）、超過判定も合算値に対して行う
    - 完了タスクに「再開」ボタンを追加（今日の日付を表示中のみ）。開始と再開は同一処理（`startTask`）で、新しい区間を開くと同一担当者の実行中タスクは自動終了（同時実行1タスクの不変条件を維持）
    - 旧形式（`startedAt`/`endedAt`直持ち）のデータは読み込み時に`getTaskSessions`が区間1つとして解釈し、いずれかの操作時に新形式へ変換して書き戻す（一括マイグレーションなし）
    - 削除可能なのは区間が1つもない行のみ（従来の「未開始のみ削除可」と同義）
  - 日報: 振り返りセクションに未完了タスク一覧・超過タスク一覧を追加
    - 選択中の担当者のその日のタスクから表示専用で導出（DB変更なし）。未完了=終了していないタスク（未開始/実行中バッジ付き）、超過=完了済みで実績が予定を上回ったタスク（予定・実績・超過分を表示）
    - 実行中で予定超過中のタスクは実績未確定のため超過一覧ではなく未完了一覧に表示
  - 進行ステージの対象案件を受注日で限定
    - 基準日`STAGE_TARGET_START_DATE = '2026-07-15'`（`constants.js`に固定値で一元定義）以降に受注（`confirmedDate`）した案件のみ進行ステージ管理の対象
    - 判定は`isStageTargetProject()`（`src/utils/stageProgress.js`）に共通化。`confirmedDate`なしの案件は対象外（安全側）
    - 対象外案件: ステッパー非表示、運用管理の遅延一覧に非掲載、ステージNAが残っていても通常NAとして扱う（完了してもステージは動かず、バッジも非表示）
    - 適用時点の対象は9件（7/17〜8/5受注）。ステージ使用中だったSparty案件（6/15受注）は対象外となり、生成済みステージNA2件は通常NAとして残置（データ変更なし）
  - 受注後の進行ステージとNA管理を双方向連動
    - ステージが次に進んだ瞬間（Done押下時）に、そのステージに対応するNAを自動生成（内容=ステージ名、期日=前ステージ完了+標準営業日数、担当者=案件の`representative`）。期限のないステージ1（契約締結）・ステージ7（プロジェクト開始）はNAを生成しない
    - 双方向同期: 運用管理のDone→NAが完了 / NA管理・案件パネルでステージNAを完了→ステージが進む / 運用管理の取消→NAが未完了（todo）に戻り、取消で無効になった次ステージの未完了NAは削除（再Doneで新期日で再生成）
    - データの正は`stageProgress.completedAt`のみ。NAの完了状態は同期される従属情報。共通関数`completeStageWithSync`/`undoStageWithSync`（`projectService.js`）に全操作経路を一本化し、常にステージを先に書いてからNAを追従させる
    - 重複生成防止（3重ガード）: ①エントリIDを決定的ID`stageNa_{ステージ番号}`+`setDoc`にし物理的に2件目を作れない ②生成前に全営業記録のエントリを走査し同`stageNaStage`が完了済み含め1件でもあればスキップ ③生成はDone押下・NA完了の明示操作時のみ（レンダリング中のuseEffectでは生成しない）
    - ステージNAは`stageNaStage`フィールドで通常NAと区別（内容を編集しても連動は壊れない）。NA管理のカード・詳細に「ステージ連動」バッジ表示
    - NA管理でステージNAのtodo/本日必達/レビュー待ち間の移動は自由（ステージに影響なし）。done→他列はステージ取消として扱い（confirmあり）、直前完了ステージ以外はアラートで据え置き。先のステージのNAを先に完了することは不可
    - ステージNAの手動削除はステージに影響しない（進行状態は維持、削除NAは復活しない）
    - 既存の運用中案件: 2026年8月6日に一回限りのバックフィルスクリプトを実行済み（対象はステージ2〜6で進行中だった1件のみ=Sparty案件にステージ3のNAを1件生成、`stageNaBackfilled: true`を記録）。アプリ内に自動バックフィル処理はない。ステージ未開始の案件は「契約締結」のDone時点から自動連動が始まる
  - 運用管理: 案件パネルに「契約・請求管理」セクションを追加（自社→クライアントの請求額をDBに一元化）
    - 既存の「月次売上管理」（クライアントの商品売上=施策成果）とは別概念のため、運用者向けタブ内に独立セクションとして追加。既存機能は変更なし
    - 支払条件（締め日・支払サイト、自由入力テキスト）+ 契約期間の配列（開始日/終了日/単価/単価の対象/月次予算額/予算の扱い）+ 各期間内の月次実績配列（年月/実績数量/手動上書き額）を`progressDashboard.billing`に保存
    - 請求額は保存せず表示時に都度計算: 数量×単価を1円未満切り捨て、「上限厳守」なら月次予算額で頭打ち（「上限適用」バッジ表示）、「超過可」はそのまま。計算ロジックは`src/utils/billing.js`
    - 請求額欄は直接編集で手動上書き可（`manualAmount`に保存、「手動調整」バッジ+「自動に戻す」ボタン、空にすると自動計算に復帰）。数量入力で即時再計算表示
    - 期間ごとの合計・案件全体の請求合計を表示。期間・月の追加/削除可（削除はconfirmあり）。金額はすべて税抜、消費税の計算・表記なし
    - 表示は進行ステージと同様に運用管理から開いた場合のみ（`showBilling` prop、既存案件一覧やNA管理から開いた場合は非表示）
    - `billing`未設定の既存案件は「未登録」表示でエラーにならない（マイグレーション不要）。編集権限の制御なし（全員編集可）
    - コンポーネントは`src/components/ContractBillingSection.js`、予算の扱い選択肢は`constants.js`の`BUDGET_POLICIES`
  - 受注（フェーズ8）時の二重登録バグを修正: 受注確定時に新規側ドキュメントを残したまま既存側の複製ドキュメントを作っていたのを廃止し、同一ドキュメントを`isExistingProject: true`に更新して既存案件へ移行する方式に変更（受注モーダル・新規追加・CSV取込の3経路とも）。案件IDが変わらないためアクションログ等の参照も維持され、`proposalMenu`などのフィールドも失われない。※過去に作られた新規側フェーズ8の残骸ドキュメント（既存側と対のもの）はDBに残っているため、不要なら新規案件一覧から手動削除が必要
  - 運用管理: 受注後の進行ステージ機能を追加
    - 7ステージ（契約締結→サンプル送付→キックオフmtg調整打診→企画会議調整打診→撮影→初稿の共有→プロジェクト開始）を順に進行。ステージ3・4は日程確定時点で完了扱い
    - 標準日数は`src/data/constants.js`の`PROJECT_STAGES`に一元定義（営業日=土日除外で計算、ToDo管理の営業日計算を`src/utils/businessDays.js`に共通化して流用）
    - 期限は保存せず「前ステージ完了日時+標準営業日数」で都度計算
    - 完了日時はDoneボタン押下時のserverTimestampのみで記録（ユーザーによる直接編集は不可）
    - 取り消しは直前に完了したステージのみ可能（完了日時をnullに戻し現在地も1つ戻る）
    - 案件パネル上部にステッパー表示（運用管理から開いた場合のみ、`showStageProgress` propで制御）。期限超過時は赤で「◯日超過」表示
    - 運用管理画面の最上部に「遅延中の案件」セクション（期限超過案件のみ、0件時は非表示、クリックでパネルを開く）
    - `stageProgress`未設定の既存案件は「ステージ1・完了なし」として扱い、ステージ1のDoneで初期化（マイグレーション不要、遅延一覧には載らない）
  - デイリータイマー: 画面表示名を「日報」に変更（ナビタブ・パンくず・ページ見出し。ルート`/daily-timer`やコレクション名`dailyTimers`は変更なし）
  - デイリータイマー: タスクに予定開始時刻（`plannedStartTime`、"HH:MM"、任意）を追加
    - 一覧は予定開始時刻の昇順で先に表示し、時刻なしの行は後ろに追加順で表示
    - 時刻+予定時間があれば「予定 9:00-9:30」のように終了予定も表示
    - 開始済みの行は「予定9:00 / 開始9:13」形式で予定と実績のズレを併記
    - 既存データ（plannedStartTimeなし）はnull扱いで動作
  - デイリータイマー: 予定時間を任意化（予定なし=nullで追加可能、予定なし行は超過判定なしで実績のみ表示、15/30/60/90分ボタンは再押下で選択解除）
  - デイリータイマー: 1日1件の振り返り欄を追加（達成できなかったこと/時間の使い方/振り返り/NAの4項目、明示的な保存ボタン、未保存変更の表示、`review`オブジェクトとして保存）
    - 振り返り欄は折りたたみ式（初期は閉、保存済みの振り返りがある日は初期展開）
  - デイリータイマー: ミニカレンダーによる過去日への移動（月切替、データがある日にドット表示）
  - デイリータイマー: 担当者プルダウンを荒幡のみに限定（担当者マスターは他画面と共用のため画面側でフィルタ）
  - デイリータイマー機能を新規追加（予定と実績のズレを可視化）
    - タブ「日報」（`/daily-timer`）を追加
    - 日付切り替え（前日/翌日矢印、今日/明日への1タップジャンプ）
    - タスク行の追加: タスク名 + 予定時間（15/30/60/90分ボタン + 任意分数入力）、担当者はプルダウン選択（担当者マスターの営業者リスト、前回選択をlocalStorageに記憶）
    - 実績はボタン押下時刻のみDBに保存し、経過時間は「現在時刻 - 開始時刻」で毎回計算（カウントアップ値は保存しない）
    - 開始時刻・終了時刻・実績時間はユーザー編集不可
    - 同時実行は1タスクのみ（別行の開始で実行中の行を自動終了）
    - 終了した行の再開は不可（続きは新しい行を追加）
    - 未開始の行のみ削除可能（開始済みの行は記録の信頼性を守るため削除不可）
    - 予定時間超過で行を赤背景 + 「超過◯分」表示（実行中・完了済みとも）
    - 全担当者の当日分を担当者別セクションで一覧表示
  - 週報機能: KGI/KPI累計を四半期通算で正しく計算するよう修正
    - 先週の累計 + 今週の実績 = 今週の累計
    - `kgiCumulatives`、`kpiCumulatives`フィールドを週報データに保存
    - 自動集計KGIは従来通り四半期データから自動計算
  - リードタイム・失注分析機能: 案件の成約/失注時にリードタイム（日数）を自動記録
    - 成約時: 登録日から成約日までの日数を`leadTimeDays`に記録
    - 失注時: 登録日から失注日までの日数を`leadTimeDays`に記録、直前フェーズを`lostAtPhase`に記録、失注日を`lostDate`に記録
  - ホーム画面: 担当者別案件サマリー（フェーズ2〜7）セクションを追加
    - 担当者プルダウン選択（増田 陽、荒幡 輝）
    - フェーズ別案件数を逆ピラミッド型ファネルチャートで表示
    - フェーズ別想定予算合計を逆ピラミッド型ファネルチャートで表示
    - 選択した担当者の案件一覧（会社名、フェーズバッジ、想定予算）
    - 各フェーズの色はSTATUS_COLORSと一致
  - 週報機能: 今週の定量アクションから目標値入力欄を削除
  - 週報機能: 先週の定量アクション結果を前週から自動引き継ぎ（◯△×評価と備考のみ入力）
  - 週報機能: 振り返り欄の拡大（600px）、文字数制限撤廃
  - 週報機能: 週切り替え時のデータ再読み込み修正
  - 案件一覧: 次回アクション日付のバッジ表示（2日以内「急」、超過「超過」）

## 開発時の注意事項
- Firestoreクエリは複合インデックスを避け、クライアントサイドでフィルタリング
- 管理者/パートナー画面の判定は `pathname.includes('/partner')` で行う
- AI分析は50文字以上の議事録で利用可能（APIキーは環境変数から取得）
- 営業日計算は土日を除外（祝日対応なし）
- 受注案件は次回アクションが存在しないビジネスルール
