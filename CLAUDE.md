# 営業管理ツール vol3 - 仕様書

## プロジェクト概要
React.js + Firebase で構築された営業進捗管理システム。管理者用とパートナー用の2つの独立したアプリケーションを提供。

## 技術スタック
- **フロントエンド**: React 19.1.0, styled-components, react-router-dom
- **バックエンド**: Firebase (Firestore, Hosting)
- **AI機能**: OpenAI GPT-4o-mini（議事録分析）
- **認証**: SHA256暗号化、セッション管理
- **デプロイ**: Firebase Hosting

## アクセス情報
- **管理者用**: https://sales-management-staging.web.app (ID: salessenjin / PW: salessenjin1234)
- **パートナー用**: https://sales-management-staging.web.app/partner/ (ID: salessenjinpiala / PW: salessenjinpiala1234)

## 主要機能

### 1. 営業管理機能
- **ホームダッシュボード**:
  - チーム全体四半期売上実績（目標達成率メーター）
  - 四半期内月別売上実績（棒グラフ）
  - チーム全体売上見込み（担当者別円グラフ）
  - 個人四半期/月間売上
  - **担当者別案件サマリー（フェーズ2〜7）**: 担当者選択式、逆ピラミッド型ファネルチャート（案件数・予算）、案件一覧
  - 滞留商談リスト（90日以上経過）
  - メニュー別/会社別実績サマリー
- **アクションログ記録**: 商談履歴の記録、AI議事録分析
- **案件一覧**: 進捗状況の管理、フィルター機能（複数選択対応）、受注案件の次回アクション日付非表示
  - 次回アクション日付バッジ: 2日以内は「急」（赤）、超過は「超過」（紫）
- **カンバンボード**: ステータス別の案件管理（ドラッグ&ドロップ機能は削除）
- **成約案件管理**: 確定日ベースの売上管理

### 2. 案件管理機能（第一想起取れるくん専用）
- **投稿本数管理**: 月別・日別の投稿数記録、受注案件のみ表示、実施月ベース自動登録
- **投稿カレンダー**: 全体スケジュールの可視化
- **ToDo管理**: 営業日ベースのタスク自動生成、期限切れアラート表示、完了済みタスクの「タスクDone」セクション
- **週報**: 四半期KGI/KPI設定と週次進捗入力、サービス別実績の自動集計

### 3. マスター管理
- **紹介者マスター**: 紹介者情報の管理
- **提案メニューマスター**: 提案メニューのカスタマイズ
- **インフルエンサー管理**: CSV一括登録対応
- **キャスティング管理**: IFキャスティング案件の進捗管理

### 4. パートナー専用機能
- 部署別管理（Buzz、コンサル、デジコン、マーケD）
- 担当者別目標管理（月別目標件数・想定遷移率）
- 紫系デザインの独立したUI

## Firestore データ構造

### コレクション一覧
```javascript
// 案件進捗
progressDashboard: {
  productName: string,          // 商材名
  proposalMenu: string,         // 提案メニュー
  representative: string,       // 社内担当者
  partnerRepresentative: string,// パートナー担当者
  introducer: string,          // 紹介者
  status: string,              // ステータス
  confirmedDate: string,       // 確定日（成約日）
  sub_department_name: string, // 部署名
  sub_department_owner: string,// 他部署担当者
  createdAt: timestamp,        // 案件登録日
  leadTimeDays: number,        // リードタイム（登録〜成約/失注までの日数）
  lostAtPhase: string,         // 失注時の直前フェーズ（失注案件のみ）
  lostDate: string,            // 失注日（失注案件のみ）
  stageProgress: {             // 受注後の進行ステージ（運用管理・既存案件のみ。confirmedDate >= 2026-07-15の案件のみ対象）
    currentStage: number,      // 現在のステージ（1〜7）
    completedAt: {             // ステージ番号 → 完了日時（Done押下時のserverTimestamp）
      [stageNo]: timestamp | null
    }
  },
  stageNaBackfilled: boolean,  // ステージ連動NAのバックフィル実行済みフラグ（2026年8月の一回限りスクリプトで付与）
  // ステージ連動NA: 最新salesRecordのentriesに決定的ID "stageNa_{ステージ番号}" で保存。
  // エントリに stageNaStage: number を持ち、通常NAと区別する（内容文字列でのマッチはしない）
  billing: {                   // 契約・請求管理（運用管理のみ。自社→クライアントの請求額管理）
    paymentTerms: {
      closingDay: string,      // 締め日（自由入力: "月末"、"15日"など）
      paymentSite: string      // 支払サイト（自由入力: "翌月末"など）
    },
    contracts: [{              // 契約期間（複数、表示は開始日昇順）
      id: string,              // 期間の一意ID
      startDate: string,       // 開始日 "YYYY-MM-DD"
      endDate: string,         // 終了日（未定は空文字）
      unitPrice: number,       // 単価（円・小数可）
      unitTarget: string,      // 単価の対象（自由入力: "再生数"など）
      monthlyBudget: number,   // 月次予算額（円）
      budgetPolicy: string,    // "上限厳守" | "超過可"（BUDGET_POLICIES）
      actuals: [{              // 月次実績（複数、表示は年月昇順）
        yearMonth: string,     // "2025-10"形式
        quantity: number,      // 実績数量（再生数など）
        manualAmount: number | null // 手動上書き請求額。null=自動計算
        // 自動計算の請求額は保存しない（数量×単価を1円未満切り捨て、上限厳守なら予算で頭打ち、で都度計算）
      }]
    }]
  },
  // ...
}

// アクションログ
actionLogs: {
  dealId: string,              // 案件ID
  productName: string,         // 商材名
  action: string,              // アクション内容
  description: string,         // 議事録
  summary: string,             // AI要約
  // ...
}

// 投稿スケジュール（第一想起取れるくん専用）
postingSchedules: {
  dealId: string,              // 案件ID
  dealName: string,            // 案件名
  yearMonth: string,           // "2025-07"形式
  schedules: { [day]: number } // 日別投稿数
}

// タスクテンプレート（第一想起取れるくん専用）
taskTemplates: {
  dealId: string,              // 案件ID
  receivedOrderDate: string,   // 受注日（確定日）
  startDate: string,           // 開始予定日
  tasks: [{                    // タスクリスト（7個の固定タスク）
    name: string,              // タスク名
    taskDate: string,          // 実施予定日
    completed: boolean,        // 完了フラグ
    completedDate: string      // 完了日時
  }]
}

// 週報設定（四半期KGI/KPI）
weeklyReportSettings: {
  quarterId: string,           // "2025-Q4"形式
  kgis: [{
    id: string,
    name: string,              // KGI名
    target: number,            // 四半期目標値
    unit: string,              // 単位（円、件など）
    autoAggregate: boolean,    // 自動集計フラグ
    aggregateService: string,  // 集計対象サービス
    aggregateMetric: string,   // 集計指標（sales, closedCount等）
    kpis: [{                   // 紐付くKPI
      id: string,
      name: string,
      target: number,
      unit: string
    }]
  }]
}

// 週報データ
weeklyReports: {
  weekId: string,              // "2025-11-18"形式（火曜日の日付）
  weekStart: string,           // 週の開始日時
  weekEnd: string,             // 週の終了日時
  kgiValues: { [kgiId]: number }, // KGI今週実績
  kpiValues: { [kpiId]: number }, // KPI今週実績
  kgiCumulatives: { [kgiId]: number }, // KGI四半期累計（先週累計+今週実績）
  kpiCumulatives: { [kpiId]: number }, // KPI四半期累計（先週累計+今週実績）
  actions: [{                  // 今週の定量アクション（目標値なし）
    id: string,
    content: string            // アクション内容のみ
  }],
  previousActionResults: [{    // 先週アクションの結果（先週のactionsから自動引き継ぎ）
    id: string,
    content: string,           // 先週のアクション内容
    result: string,            // ◯、△、×
    resultNote: string         // 備考
  }],
  review: string,              // 振り返り（文字数制限なし）
  updatedAt: string
}

// デイリータイマー（担当者×日付で1ドキュメント、ID: "{担当者名}_{YYYY-MM-DD}"）
dailyTimers: {
  representative: string,      // 担当者名
  date: string,                // "2026-07-31"形式
  tasks: [{                    // タスク行
    id: string,                // 行ID
    name: string,              // タスク名
    plannedMinutes: number,    // 予定時間（分・整数、予定なしはnull）
    plannedStartTime: string,  // 予定開始時刻（"HH:MM"、未設定はnull。実績とは別物で編集可）
    sessions: [{               // 作業区間（時系列順）。再開のたびに区間が増える
      startedAt: timestamp,    // 区間の開始（開始/再開ボタン押下時刻）
      endedAt: timestamp       // 区間の終了（実行中はnull。開いている区間は常に最後の1つのみ）
    }]
    // 旧形式はstartedAt/endedAt直持ち（区間1つとして解釈し、いずれかの操作時に新形式へ変換）
  }],
  review: {                    // 1日1件の振り返り（tasksとは独立、担当者×日付ごと）
    notAchieved: string,       // 達成できなかったことはないか？なぜか？どう組み直すか？
    timeImprovement: string,   // 時間の使い方をもっとよくすることはできないか？
    reflection: string,        // 振り返り
    nextAction: string         // NA（明日のネクストアクション）
  },
  updatedAt: timestamp
  // 実績時間は保存しない（閉じた作業区間の合算を都度計算する）
}
```

## ビルド・デプロイ

```bash
# 両アプリ同時ビルド
./build-all.sh

# デプロイ
firebase deploy --only hosting --project sales-management-staging

# 個別ビルド
npm run build                    # 管理者用
REACT_APP_ENTRY_POINT=partner npm run build  # パートナー用
```

## 最新バージョン情報
- **現在バージョン**: v2.40.0
- **最終更新**: 2026年8月10日
- **直近の更新内容**:
  - 日報: 各タスク行に「＋」ボタン（行直下へのタスク差し込み）を追加
    - 押すとその行の直下にインライン入力欄（開始時刻・タスク名・所要時間）が開く。開始時刻には押した行の予定終了時刻（予定開始+予定時間）を自動入力、計算できない行（予定なし等）は空で開き手入力（開始時刻・タスク名・所要時間すべて必須）
    - 追加は既存の`addTask`をそのまま使用（サービス層変更なし）。追加先は押した行の担当者。既存タスクの予定には影響しない
    - 同時に開けるフォームは1つ（時刻編集フォームとも排他）。並び順は既存の予定開始時刻昇順に従う
  - 日報: 完了タスク行の右端に妥当性の◯×を自動表示
    - 実績が予定時間以内=◯（緑）、超過=×（赤）、予定時間なし=−（グレー）。未開始・実行中は表示しない
    - 判定は超過バッジ・超過タスク一覧と同じ`getTaskTiming`の`overdue`を使用（表示間で矛盾しない）。手動変更は不可、保存もしない（表示のみ）
  - 日報: 割り込みタスクの「今すぐ開始」ボタンを追加
    - 「タスクを追加」フォームの「追加」ボタン横に緑の「今すぐ開始」ボタン（今日の日付を表示中のみ）。押すと現在時刻を開始時刻としてタスクが追加され、即実行中になる（`addTaskAndStart`）
    - 予定開始時刻は使わない（`plannedStartTime: null`で保存、既存タスクの予定にも影響なし）。予定時間はフォームに入力されていれば使う
    - 実行中のタスクは自動終了（同時実行1タスクの不変条件を維持）。中断されたタスクは「再開」で戻れる
  - 日報: 作業区間の開始・終了時刻の手動修正機能を追加（タイマー押し忘れの事後修正用）
    - 各行の編集アイコン（全状態・過去日でも表示）からインライン編集。区間ごとに開始・終了の時刻（"HH:MM"、その日付内、秒は00）を修正でき、実績は修正後の区間から都度再計算
    - 実行中タスクは開始時刻の修正に加え、終了時刻を入力するとその時刻で完了（空のままなら実行中を継続）
    - 未開始タスクは開始・終了の両方を入力すると完了として記録。手動修正で実行中状態は作らない
    - バリデーション（`updateTaskSessions`）: 各区間で終了>開始、区間同士は時系列順で重複不可、日またぎ不可。区間の追加・削除は編集ではできない（区間を増やしたい場合は「再開」してから時刻を修正する運用）
  - 日報: 終了したタスクの再開機能を追加
    - タスクのデータモデルを作業区間の配列`sessions: [{startedAt, endedAt}]`に変更。実績時間は閉じた区間の合算を都度計算（保存しない設計は維持）、超過判定も合算値に対して行う
    - 完了タスクに「再開」ボタンを追加（今日の日付を表示中のみ）。開始と再開は同一処理（`startTask`）で、新しい区間を開くと同一担当者の実行中タスクは自動終了（同時実行1タスクの不変条件を維持）
    - 旧形式（`startedAt`/`endedAt`直持ち）のデータは読み込み時に`getTaskSessions`が区間1つとして解釈し、いずれかの操作時に新形式へ変換して書き戻す（一括マイグレーションなし）
    - 削除可能なのは区間が1つもない行のみ（従来の「未開始のみ削除可」と同義）
  - 日報: 振り返りセクションに未完了タスク一覧・超過タスク一覧を追加
    - 選択中の担当者のその日のタスクから表示専用で導出（DB変更なし）。未完了=終了していないタスク（未開始/実行中バッジ付き）、超過=完了済みで実績が予定を上回ったタスク（予定・実績・超過分を表示）
    - 実行中で予定超過中のタスクは実績未確定のため超過一覧ではなく未完了一覧に表示
  - 進行ステージの対象案件を受注日で限定
    - 基準日`STAGE_TARGET_START_DATE = '2026-07-15'`（`constants.js`に固定値で一元定義）以降に受注（`confirmedDate`）した案件のみ進行ステージ管理の対象
    - 判定は`isStageTargetProject()`（`src/utils/stageProgress.js`）に共通化。`confirmedDate`なしの案件は対象外（安全側）
    - 対象外案件: ステッパー非表示、運用管理の遅延一覧に非掲載、ステージNAが残っていても通常NAとして扱う（完了してもステージは動かず、バッジも非表示）
    - 適用時点の対象は9件（7/17〜8/5受注）。ステージ使用中だったSparty案件（6/15受注）は対象外となり、生成済みステージNA2件は通常NAとして残置（データ変更なし）
  - 受注後の進行ステージとNA管理を双方向連動
    - ステージが次に進んだ瞬間（Done押下時）に、そのステージに対応するNAを自動生成（内容=ステージ名、期日=前ステージ完了+標準営業日数、担当者=案件の`representative`）。期限のないステージ1（契約締結）・ステージ7（プロジェクト開始）はNAを生成しない
    - 双方向同期: 運用管理のDone→NAが完了 / NA管理・案件パネルでステージNAを完了→ステージが進む / 運用管理の取消→NAが未完了（todo）に戻り、取消で無効になった次ステージの未完了NAは削除（再Doneで新期日で再生成）
    - データの正は`stageProgress.completedAt`のみ。NAの完了状態は同期される従属情報。共通関数`completeStageWithSync`/`undoStageWithSync`（`projectService.js`）に全操作経路を一本化し、常にステージを先に書いてからNAを追従させる
    - 重複生成防止（3重ガード）: ①エントリIDを決定的ID`stageNa_{ステージ番号}`+`setDoc`にし物理的に2件目を作れない ②生成前に全営業記録のエントリを走査し同`stageNaStage`が完了済み含め1件でもあればスキップ ③生成はDone押下・NA完了の明示操作時のみ（レンダリング中のuseEffectでは生成しない）
    - ステージNAは`stageNaStage`フィールドで通常NAと区別（内容を編集しても連動は壊れない）。NA管理のカード・詳細に「ステージ連動」バッジ表示
    - NA管理でステージNAのtodo/本日必達/レビュー待ち間の移動は自由（ステージに影響なし）。done→他列はステージ取消として扱い（confirmあり）、直前完了ステージ以外はアラートで据え置き。先のステージのNAを先に完了することは不可
    - ステージNAの手動削除はステージに影響しない（進行状態は維持、削除NAは復活しない）
    - 既存の運用中案件: 2026年8月6日に一回限りのバックフィルスクリプトを実行済み（対象はステージ2〜6で進行中だった1件のみ=Sparty案件にステージ3のNAを1件生成、`stageNaBackfilled: true`を記録）。アプリ内に自動バックフィル処理はない。ステージ未開始の案件は「契約締結」のDone時点から自動連動が始まる
  - 運用管理: 案件パネルに「契約・請求管理」セクションを追加（自社→クライアントの請求額をDBに一元化）
    - 既存の「月次売上管理」（クライアントの商品売上=施策成果）とは別概念のため、運用者向けタブ内に独立セクションとして追加。既存機能は変更なし
    - 支払条件（締め日・支払サイト、自由入力テキスト）+ 契約期間の配列（開始日/終了日/単価/単価の対象/月次予算額/予算の扱い）+ 各期間内の月次実績配列（年月/実績数量/手動上書き額）を`progressDashboard.billing`に保存
    - 請求額は保存せず表示時に都度計算: 数量×単価を1円未満切り捨て、「上限厳守」なら月次予算額で頭打ち（「上限適用」バッジ表示）、「超過可」はそのまま。計算ロジックは`src/utils/billing.js`
    - 請求額欄は直接編集で手動上書き可（`manualAmount`に保存、「手動調整」バッジ+「自動に戻す」ボタン、空にすると自動計算に復帰）。数量入力で即時再計算表示
    - 期間ごとの合計・案件全体の請求合計を表示。期間・月の追加/削除可（削除はconfirmあり）。金額はすべて税抜、消費税の計算・表記なし
    - 表示は進行ステージと同様に運用管理から開いた場合のみ（`showBilling` prop、既存案件一覧やNA管理から開いた場合は非表示）
    - `billing`未設定の既存案件は「未登録」表示でエラーにならない（マイグレーション不要）。編集権限の制御なし（全員編集可）
    - コンポーネントは`src/components/ContractBillingSection.js`、予算の扱い選択肢は`constants.js`の`BUDGET_POLICIES`
  - 受注（フェーズ8）時の二重登録バグを修正: 受注確定時に新規側ドキュメントを残したまま既存側の複製ドキュメントを作っていたのを廃止し、同一ドキュメントを`isExistingProject: true`に更新して既存案件へ移行する方式に変更（受注モーダル・新規追加・CSV取込の3経路とも）。案件IDが変わらないためアクションログ等の参照も維持され、`proposalMenu`などのフィールドも失われない。※過去に作られた新規側フェーズ8の残骸ドキュメント（既存側と対のもの）はDBに残っているため、不要なら新規案件一覧から手動削除が必要
  - 運用管理: 受注後の進行ステージ機能を追加
    - 7ステージ（契約締結→サンプル送付→キックオフmtg調整打診→企画会議調整打診→撮影→初稿の共有→プロジェクト開始）を順に進行。ステージ3・4は日程確定時点で完了扱い
    - 標準日数は`src/data/constants.js`の`PROJECT_STAGES`に一元定義（営業日=土日除外で計算、ToDo管理の営業日計算を`src/utils/businessDays.js`に共通化して流用）
    - 期限は保存せず「前ステージ完了日時+標準営業日数」で都度計算
    - 完了日時はDoneボタン押下時のserverTimestampのみで記録（ユーザーによる直接編集は不可）
    - 取り消しは直前に完了したステージのみ可能（完了日時をnullに戻し現在地も1つ戻る）
    - 案件パネル上部にステッパー表示（運用管理から開いた場合のみ、`showStageProgress` propで制御）。期限超過時は赤で「◯日超過」表示
    - 運用管理画面の最上部に「遅延中の案件」セクション（期限超過案件のみ、0件時は非表示、クリックでパネルを開く）
    - `stageProgress`未設定の既存案件は「ステージ1・完了なし」として扱い、ステージ1のDoneで初期化（マイグレーション不要、遅延一覧には載らない）
  - デイリータイマー: 画面表示名を「日報」に変更（ナビタブ・パンくず・ページ見出し。ルート`/daily-timer`やコレクション名`dailyTimers`は変更なし）
  - デイリータイマー: タスクに予定開始時刻（`plannedStartTime`、"HH:MM"、任意）を追加
    - 一覧は予定開始時刻の昇順で先に表示し、時刻なしの行は後ろに追加順で表示
    - 時刻+予定時間があれば「予定 9:00-9:30」のように終了予定も表示
    - 開始済みの行は「予定9:00 / 開始9:13」形式で予定と実績のズレを併記
    - 既存データ（plannedStartTimeなし）はnull扱いで動作
  - デイリータイマー: 予定時間を任意化（予定なし=nullで追加可能、予定なし行は超過判定なしで実績のみ表示、15/30/60/90分ボタンは再押下で選択解除）
  - デイリータイマー: 1日1件の振り返り欄を追加（達成できなかったこと/時間の使い方/振り返り/NAの4項目、明示的な保存ボタン、未保存変更の表示、`review`オブジェクトとして保存）
    - 振り返り欄は折りたたみ式（初期は閉、保存済みの振り返りがある日は初期展開）
  - デイリータイマー: ミニカレンダーによる過去日への移動（月切替、データがある日にドット表示）
  - デイリータイマー: 担当者プルダウンを荒幡のみに限定（担当者マスターは他画面と共用のため画面側でフィルタ）
  - デイリータイマー機能を新規追加（予定と実績のズレを可視化）
    - タブ「日報」（`/daily-timer`）を追加
    - 日付切り替え（前日/翌日矢印、今日/明日への1タップジャンプ）
    - タスク行の追加: タスク名 + 予定時間（15/30/60/90分ボタン + 任意分数入力）、担当者はプルダウン選択（担当者マスターの営業者リスト、前回選択をlocalStorageに記憶）
    - 実績はボタン押下時刻のみDBに保存し、経過時間は「現在時刻 - 開始時刻」で毎回計算（カウントアップ値は保存しない）
    - 開始時刻・終了時刻・実績時間はユーザー編集不可
    - 同時実行は1タスクのみ（別行の開始で実行中の行を自動終了）
    - 終了した行の再開は不可（続きは新しい行を追加）
    - 未開始の行のみ削除可能（開始済みの行は記録の信頼性を守るため削除不可）
    - 予定時間超過で行を赤背景 + 「超過◯分」表示（実行中・完了済みとも）
    - 全担当者の当日分を担当者別セクションで一覧表示
  - 週報機能: KGI/KPI累計を四半期通算で正しく計算するよう修正
    - 先週の累計 + 今週の実績 = 今週の累計
    - `kgiCumulatives`、`kpiCumulatives`フィールドを週報データに保存
    - 自動集計KGIは従来通り四半期データから自動計算
  - リードタイム・失注分析機能: 案件の成約/失注時にリードタイム（日数）を自動記録
    - 成約時: 登録日から成約日までの日数を`leadTimeDays`に記録
    - 失注時: 登録日から失注日までの日数を`leadTimeDays`に記録、直前フェーズを`lostAtPhase`に記録、失注日を`lostDate`に記録
  - ホーム画面: 担当者別案件サマリー（フェーズ2〜7）セクションを追加
    - 担当者プルダウン選択（増田 陽、荒幡 輝）
    - フェーズ別案件数を逆ピラミッド型ファネルチャートで表示
    - フェーズ別想定予算合計を逆ピラミッド型ファネルチャートで表示
    - 選択した担当者の案件一覧（会社名、フェーズバッジ、想定予算）
    - 各フェーズの色はSTATUS_COLORSと一致
  - 週報機能: 今週の定量アクションから目標値入力欄を削除
  - 週報機能: 先週の定量アクション結果を前週から自動引き継ぎ（◯△×評価と備考のみ入力）
  - 週報機能: 振り返り欄の拡大（600px）、文字数制限撤廃
  - 週報機能: 週切り替え時のデータ再読み込み修正
  - 案件一覧: 次回アクション日付のバッジ表示（2日以内「急」、超過「超過」）

## 開発時の注意事項
- Firestoreクエリは複合インデックスを避け、クライアントサイドでフィルタリング
- 管理者/パートナー画面の判定は `pathname.includes('/partner')` で行う
- AI分析は50文字以上の議事録で利用可能（APIキーは環境変数から取得）
- 営業日計算は土日を除外（祝日対応なし）
- 受注案件は次回アクションが存在しないビジネスルール
